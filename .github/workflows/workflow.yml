name: PipelineWorkflow

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  trigger-azure-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Azure DevOps Pipeline
        uses: azure/pipelines@v1
        with:
          azure-devops-project-url: 'https://dev.azure.com/Darshana0153/Ascendion-Darshana'
          azure-pipeline-name: 'ascendion-darshana1'
          azure-devops-token: ${{ secrets.AZURE_DEVOPS_TOKEN }}
